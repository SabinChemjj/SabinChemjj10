#include <stdio.h>

int main() 
{
    double r, h, v;
    printf("Enter radius and height: ");
    scanf("%lf %lf", &r, &h);
    v = 3.14159 * r * r * h;
    printf("Volume = %.2lf", v);
    return 0;
}
